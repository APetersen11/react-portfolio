// contact form with fields for name, email, and message
// notification that info is required
// email validation
import React, { Component } from 'react'

export default class index extends Component {
  render() {
    return (
      <div>Contact</div>
    )
  }
}
